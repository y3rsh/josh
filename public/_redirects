# Cross-domain canonical redirects are configured in Cloudflare Page Rules per zone.
# Pages _redirects only supports relative paths on the same hostname (see developers.cloudflare.com/pages/configuration/redirects).
